import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebarsIndexers: SidebarsConfig = {
    indexersSidebar: [
        'introduction',
    ],
  };
  
  export default sidebarsIndexers;
  